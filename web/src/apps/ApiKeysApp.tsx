import { AppShell, Empty } from "./shell";

export function ApiKeysApp() {
  return (
    <AppShell title="API Keys" subtitle="Gateway keys & limits">
      <Empty message="API keys management coming up." />
    </AppShell>
  );
}
